services:
  main:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        UID: ${USER_ID:?required}
        GID: ${GROUP_ID:?required}
    tty: true
    stdin_open: true
    volumes:
      - claude_config:/home/node/.claude
    command: "claude"

volumes:
  claude_config:
